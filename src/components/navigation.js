import React from "react";
import styled from "styled-components";

const Wrap = styled.div`
  max-width: 935px;
  background: green;
  margin: 0 auto;
`;

export function Navigation() {
  return <Wrap>Navigation</Wrap>;
}
